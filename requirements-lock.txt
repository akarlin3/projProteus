# Proteus — resolved Python deps lock. GENERATED, DO NOT HAND-EDIT.
#
# ┌─ STATUS: PENDING GENERATION ON THE M4 ──────────────────────────────────────┐
# │ This lockfile must be a `pip freeze` of the resolved osx-arm64 env. It was   │
# │ NOT generated here: the scaffolding session ran in a Linux x86_64 container  │
# │ (no Apple Silicon, no MPS), so any freeze produced here would pin the WRONG  │
# │ platform wheels (e.g. a linux torch build instead of the arm64 MPS build).   │
# │ Fabricating arm64 pins would be dishonest, so this file is intentionally     │
# │ empty of pins. See envlog/env-failures.md for the full reason.               │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Generate the REAL lock on the M4 MacBook Air after a clean install:
#
#   CONDA_SUBDIR=osx-arm64 mamba env create -f environment.yml
#   conda activate proteus
#   # verify imports/runs (Checkpoint 2) and pass the smoke suite (Checkpoint 3):
#   pytest tests/test_smoke.py -v
#   # then freeze:
#   pip freeze > requirements-lock.txt
#   conda env export > envlog/conda-env-resolved.yml
#
# Expected top-level packages once resolved (versions filled by the freeze, not by hand):
#   torch (arm64, MPS)  transformers  sentencepiece  meeko
#   numpy  pandas  biopython  biotite  pyyaml  tqdm  pytest
#   (foldseek / mmseqs2 / fpocket / vina are conda/Homebrew tools, not pip — see
#    environment.yml and envlog/conda-env-resolved.yml)
#
# DO NOT add esmfold / fair-esm / chai_lab / gnina — those are Vast-only (vast/).
